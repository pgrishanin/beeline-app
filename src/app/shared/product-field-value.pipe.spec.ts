import { ProductFieldValuePipe } from './product-field-value.pipe';

describe('ProductFieldValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFieldValuePipe();
    expect(pipe).toBeTruthy();
  });
});
